name: Synchronization

on:
    push:
        branches:
            - main
    workflow_dispatch:

permissions: write-all

jobs:
    create:
        uses: Laravel-Lang/.github/.github/workflows/sync.yml@main
