name: Release Drafter

on:
    push:
        branches:
            - main
    release:
        types:
            - unpublished
    workflow_dispatch:

permissions:
    contents: write
    pull-requests: write

jobs:
    Update:
        uses: Laravel-Lang/.github/.github/workflows/release-drafter.yml@main
